[build]
  command = "npm run build"
  publish = ".output/public"
  # Usar npm install (no npm ci) para que las optionalDependencies (oxc-parser
  # bindings) se instalen para Linux en Netlify. Ver: https://github.com/npm/cli/issues/4828
  install_command = "npm install"
